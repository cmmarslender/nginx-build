diff --git a/nginx.spec b/nginx.spec
index e034726..2e13e03 100644
--- a/nginx.spec
+++ b/nginx.spec
@@ -68,6 +68,7 @@ Source9: nginx.upgrade.sh
 Source10: nginx.suse.logrotate
 Source11: nginx-debug.service
 Source12: COPYRIGHT
+Source13: ngx_pagespeed
 
 License: 2-clause BSD-like license
 
@@ -88,6 +89,7 @@ a mail proxy server.
 %prep
 %setup -q
 cp %{SOURCE2} .
+cp -R -p %{SOURCE13} .
 sed -e 's|%%DEFAULTSTART%%|2 3 4 5|g' -e 's|%%DEFAULTSTOP%%|0 1 6|g' \
     -e 's|%%PROVIDES%%|nginx|g' < %{SOURCE2} > nginx.init
 sed -e 's|%%DEFAULTSTART%%||g' -e 's|%%DEFAULTSTOP%%|0 1 2 3 4 5 6|g' \
@@ -133,6 +135,7 @@ sed -e 's|%%DEFAULTSTART%%||g' -e 's|%%DEFAULTSTOP%%|0 1 2 3 4 5 6|g' \
         --with-debug \
         %{?with_http2:--with-http_v2_module} \
         --with-cc-opt="%{optflags} $(pcre-config --cflags)" \
+	--add-module=%{_builddir}/%{name}-%{version}/ngx_pagespeed/ \
         $*
 make %{?_smp_mflags}
 %{__mv} %{_builddir}/%{name}-%{version}/objs/nginx \
@@ -175,6 +178,7 @@ make %{?_smp_mflags}
         --with-ipv6 \
         %{?with_http2:--with-http_v2_module} \
         --with-cc-opt="%{optflags} $(pcre-config --cflags)" \
+	--add-module=%{_builddir}/%{name}-%{version}/ngx_pagespeed/ \
         $*
 make %{?_smp_mflags}
 
