diff --git a/nginx.spec b/nginx.spec
index d716b22..a0e6ba9 100644
--- a/nginx.spec
+++ b/nginx.spec
@@ -56,7 +56,7 @@ Requires: systemd
 
 # end of distribution specific definitions
 
-Summary: High performance web server
+Summary: High performance web server (10up build)
 Name: nginx
 Version: 1.9.0
 Release: 1%{?dist}.ngx
@@ -73,6 +73,8 @@ Source7: nginx.suse.init
 Source8: nginx.service
 Source9: nginx.upgrade.sh
 Source10: nginx.suse.logrotate
+Source11: ngx_pagespeed
+Source12: naxsi
 
 License: 2-clause BSD-like license
 
@@ -99,6 +101,8 @@ Not stripped version of nginx built with the debugging log support.
 
 %prep
 %setup -q
+cp -R -p %SOURCE11 .
+cp -R -p %SOURCE12 .
 
 %build
 ./configure \
@@ -139,6 +143,8 @@ Not stripped version of nginx built with the debugging log support.
         --with-debug \
         %{?with_spdy:--with-http_spdy_module} \
         --with-cc-opt="%{optflags} $(pcre-config --cflags)" \
+        --add-module=%{_builddir}/%{name}-%{version}/ngx_pagespeed/ \
+        --add-module=%{_builddir}/%{name}-%{version}/naxsi/naxsi_src/ \
         $*
 make %{?_smp_mflags}
 %{__mv} %{_builddir}/%{name}-%{version}/objs/nginx \
@@ -180,6 +186,8 @@ make %{?_smp_mflags}
         --with-ipv6 \
         %{?with_spdy:--with-http_spdy_module} \
         --with-cc-opt="%{optflags} $(pcre-config --cflags)" \
+        --add-module=%{_builddir}/%{name}-%{version}/ngx_pagespeed/ \
+        --add-module=%{_builddir}/%{name}-%{version}/naxsi/naxsi_src/ \
         $*
 make %{?_smp_mflags}
 
@@ -208,6 +216,9 @@ make %{?_smp_mflags}
 %{__install} -m 644 -p %{SOURCE3} \
    $RPM_BUILD_ROOT%{_sysconfdir}/sysconfig/nginx
 
+%{__install} -m 644 -p %{SOURCE12}/naxsi_config/naxsi_core.rules \
+   $RPM_BUILD_ROOT%{_sysconfdir}/nginx/naxsi_core.rules
+
 %if %{use_systemd}
 # install systemd-specific files
 %{__mkdir} -p $RPM_BUILD_ROOT%{_unitdir}
@@ -261,6 +272,7 @@ make %{?_smp_mflags}
 %config(noreplace) %{_sysconfdir}/nginx/koi-utf
 %config(noreplace) %{_sysconfdir}/nginx/koi-win
 %config(noreplace) %{_sysconfdir}/nginx/win-utf
+%config(noreplace) %{_sysconfdir}/nginx/naxsi_core.rules
 
 %config(noreplace) %{_sysconfdir}/logrotate.d/nginx
 %config(noreplace) %{_sysconfdir}/sysconfig/nginx
